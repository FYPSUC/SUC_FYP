import 'package:flutter/material.dart';

class VendorViewOrderPage extends StatelessWidget {
  const VendorViewOrderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Voucher Page')),
      body: const Center(
        child: Text('Welcome to UserVoucher Page'),
      ),
    );
  }
}