import 'package:flutter/material.dart';

class VendorTransactionHistoryPage extends StatelessWidget {
  const VendorTransactionHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Voucher Page')),
      body: const Center(
        child: Text('Welcome to UserVoucher Page'),
      ),
    );
  }
}